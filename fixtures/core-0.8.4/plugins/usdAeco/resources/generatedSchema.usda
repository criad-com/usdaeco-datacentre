#usda 1.0
(
    "WARNING: THIS FILE IS GENERATED BY usdGenSchema.  DO NOT EDIT."
    fallbackPrimTypes = {
        token[] AecoFacility = ["Xform"]
        token[] AecoFacilityPart = ["Xform"]
        token[] AecoLevel = ["Xform"]
        token[] AecoPort = ["Xform"]
        token[] AecoSite = ["Xform"]
        token[] AecoSpace = ["Xform"]
        token[] AecoSystem = ["Scope"]
        token[] AecoZone = ["Scope"]
    }
)

class "AecoSpatialBase" (
    customData = {
        string userDocBrief = "Abstract root of all AECO spatial containers."
    }
)
{
    string aeco:id = "" (
        customData = {
            string userDocBrief = """Stable, globally unique identity (lowercase hyphenated
        UUID recommended) — the counterpart of IfcSpatialElement's
        GlobalId."""
        }
    )
    uniform token aeco:phase = "proposed" (
        allowedTokens = ["proposed", "existing", "demolished", "temporary"]
        customData = {
            string userDocBrief = """Lifecycle phase of this spatial container with respect
        to this project — the asset clock (E12) on the spatial
        structure itself, so a renovation models existing fabric and
        the proposed remodel in ONE structure: an existing wing, a
        proposed space, a temporary works enclosure."""
        }
    )
    rel proxyPrim (
        customData = {
            string userDocBrief = '''The proxyPrim relationship allows us to link a
        prim whose purpose is "render" to its (single target)
        purpose="proxy" prim.'''
        }
    )
    uniform token purpose = "default" (
        allowedTokens = ["default", "render", "proxy", "guide"]
        customData = {
            string userDocBrief = """Purpose is a classification of geometry into categories that 
        can each be independently included or excluded from traversals of prims 
        on a stage, such as rendering or bounding-box computation traversals."""
        }
    )
    token visibility = "inherited" (
        allowedTokens = ["inherited", "invisible"]
        customData = {
            string userDocBrief = '''Visibility is meant to be the simplest form of "pruning" 
        visibility that is supported by most DCC apps.'''
        }
    )
    uniform token[] xformOpOrder (
        customData = {
            string userDocBrief = """Encodes the sequence of transformation operations in the
        order in which they should be pushed onto a transform stack while
        visiting a UsdStage's prims in a graph traversal that will effect
        the desired positioning for this prim and its descendant prims."""
        }
    )
}

class AecoSite "AecoSite" (
    customData = {
        string userDocBrief = "An area of land or water under development."
    }
)
{
    string aeco:id = "" (
        customData = {
            string userDocBrief = """Stable, globally unique identity (lowercase hyphenated
        UUID recommended) — the counterpart of IfcSpatialElement's
        GlobalId."""
        }
    )
    uniform token aeco:phase = "proposed" (
        allowedTokens = ["proposed", "existing", "demolished", "temporary"]
        customData = {
            string userDocBrief = """Lifecycle phase of this spatial container with respect
        to this project — the asset clock (E12) on the spatial
        structure itself, so a renovation models existing fabric and
        the proposed remodel in ONE structure: an existing wing, a
        proposed space, a temporary works enclosure."""
        }
    )
    rel proxyPrim (
        customData = {
            string userDocBrief = '''The proxyPrim relationship allows us to link a
        prim whose purpose is "render" to its (single target)
        purpose="proxy" prim.'''
        }
    )
    uniform token purpose = "default" (
        allowedTokens = ["default", "render", "proxy", "guide"]
        customData = {
            string userDocBrief = """Purpose is a classification of geometry into categories that 
        can each be independently included or excluded from traversals of prims 
        on a stage, such as rendering or bounding-box computation traversals."""
        }
    )
    token visibility = "inherited" (
        allowedTokens = ["inherited", "invisible"]
        customData = {
            string userDocBrief = '''Visibility is meant to be the simplest form of "pruning" 
        visibility that is supported by most DCC apps.'''
        }
    )
    uniform token[] xformOpOrder (
        customData = {
            string userDocBrief = """Encodes the sequence of transformation operations in the
        order in which they should be pushed onto a transform stack while
        visiting a UsdStage's prims in a graph traversal that will effect
        the desired positioning for this prim and its descendant prims."""
        }
    )
}

class AecoFacility "AecoFacility" (
    customData = {
        string userDocBrief = """One built asset operated as a whole: a building, bridge,
    road, tunnel, plant."""
    }
)
{
    string aeco:id = "" (
        customData = {
            string userDocBrief = """Stable, globally unique identity (lowercase hyphenated
        UUID recommended) — the counterpart of IfcSpatialElement's
        GlobalId."""
        }
    )
    uniform token aeco:phase = "proposed" (
        allowedTokens = ["proposed", "existing", "demolished", "temporary"]
        customData = {
            string userDocBrief = """Lifecycle phase of this spatial container with respect
        to this project — the asset clock (E12) on the spatial
        structure itself, so a renovation models existing fabric and
        the proposed remodel in ONE structure: an existing wing, a
        proposed space, a temporary works enclosure."""
        }
    )
    rel proxyPrim (
        customData = {
            string userDocBrief = '''The proxyPrim relationship allows us to link a
        prim whose purpose is "render" to its (single target)
        purpose="proxy" prim.'''
        }
    )
    uniform token purpose = "default" (
        allowedTokens = ["default", "render", "proxy", "guide"]
        customData = {
            string userDocBrief = """Purpose is a classification of geometry into categories that 
        can each be independently included or excluded from traversals of prims 
        on a stage, such as rendering or bounding-box computation traversals."""
        }
    )
    token visibility = "inherited" (
        allowedTokens = ["inherited", "invisible"]
        customData = {
            string userDocBrief = '''Visibility is meant to be the simplest form of "pruning" 
        visibility that is supported by most DCC apps.'''
        }
    )
    uniform token[] xformOpOrder (
        customData = {
            string userDocBrief = """Encodes the sequence of transformation operations in the
        order in which they should be pushed onto a transform stack while
        visiting a UsdStage's prims in a graph traversal that will effect
        the desired positioning for this prim and its descendant prims."""
        }
    )
}

class AecoFacilityPart "AecoFacilityPart" (
    customData = {
        string userDocBrief = """A structural subdivision of a facility: wing, block,
    tower, podium, road section, track segment, process unit."""
    }
)
{
    string aeco:id = "" (
        customData = {
            string userDocBrief = """Stable, globally unique identity (lowercase hyphenated
        UUID recommended) — the counterpart of IfcSpatialElement's
        GlobalId."""
        }
    )
    uniform token aeco:phase = "proposed" (
        allowedTokens = ["proposed", "existing", "demolished", "temporary"]
        customData = {
            string userDocBrief = """Lifecycle phase of this spatial container with respect
        to this project — the asset clock (E12) on the spatial
        structure itself, so a renovation models existing fabric and
        the proposed remodel in ONE structure: an existing wing, a
        proposed space, a temporary works enclosure."""
        }
    )
    rel proxyPrim (
        customData = {
            string userDocBrief = '''The proxyPrim relationship allows us to link a
        prim whose purpose is "render" to its (single target)
        purpose="proxy" prim.'''
        }
    )
    uniform token purpose = "default" (
        allowedTokens = ["default", "render", "proxy", "guide"]
        customData = {
            string userDocBrief = """Purpose is a classification of geometry into categories that 
        can each be independently included or excluded from traversals of prims 
        on a stage, such as rendering or bounding-box computation traversals."""
        }
    )
    token visibility = "inherited" (
        allowedTokens = ["inherited", "invisible"]
        customData = {
            string userDocBrief = '''Visibility is meant to be the simplest form of "pruning" 
        visibility that is supported by most DCC apps.'''
        }
    )
    uniform token[] xformOpOrder (
        customData = {
            string userDocBrief = """Encodes the sequence of transformation operations in the
        order in which they should be pushed onto a transform stack while
        visiting a UsdStage's prims in a graph traversal that will effect
        the desired positioning for this prim and its descendant prims."""
        }
    )
}

class AecoLevel "AecoLevel" (
    customData = {
        string userDocBrief = """The one universal facility part: a horizontal stratum at a
    datum elevation — typed because every vertical facility on earth
    shares this convention and schedules/sections key on it."""
    }
)
{
    double aeco:elevation = 0 (
        customData = {
            string userDocBrief = """Declarative datum elevation in the frame of the
        nearest ancestor facility, along the stage up axis, in stage
        linear units — used for ordering, schedules and sections."""
        }
    )
    string aeco:id = "" (
        customData = {
            string userDocBrief = """Stable, globally unique identity (lowercase hyphenated
        UUID recommended) — the counterpart of IfcSpatialElement's
        GlobalId."""
        }
    )
    uniform token aeco:phase = "proposed" (
        allowedTokens = ["proposed", "existing", "demolished", "temporary"]
        customData = {
            string userDocBrief = """Lifecycle phase of this spatial container with respect
        to this project — the asset clock (E12) on the spatial
        structure itself, so a renovation models existing fabric and
        the proposed remodel in ONE structure: an existing wing, a
        proposed space, a temporary works enclosure."""
        }
    )
    rel proxyPrim (
        customData = {
            string userDocBrief = '''The proxyPrim relationship allows us to link a
        prim whose purpose is "render" to its (single target)
        purpose="proxy" prim.'''
        }
    )
    uniform token purpose = "default" (
        allowedTokens = ["default", "render", "proxy", "guide"]
        customData = {
            string userDocBrief = """Purpose is a classification of geometry into categories that 
        can each be independently included or excluded from traversals of prims 
        on a stage, such as rendering or bounding-box computation traversals."""
        }
    )
    token visibility = "inherited" (
        allowedTokens = ["inherited", "invisible"]
        customData = {
            string userDocBrief = '''Visibility is meant to be the simplest form of "pruning" 
        visibility that is supported by most DCC apps.'''
        }
    )
    uniform token[] xformOpOrder (
        customData = {
            string userDocBrief = """Encodes the sequence of transformation operations in the
        order in which they should be pushed onto a transform stack while
        visiting a UsdStage's prims in a graph traversal that will effect
        the desired positioning for this prim and its descendant prims."""
        }
    )
}

class AecoSpace "AecoSpace" (
    customData = {
        string userDocBrief = """A bounded usable region, interior or exterior — a room, a
    hall, a yard, a corridor, an atrium, a plaza."""
    }
)
{
    string aeco:id = "" (
        customData = {
            string userDocBrief = """Stable, globally unique identity (lowercase hyphenated
        UUID recommended) — the counterpart of IfcSpatialElement's
        GlobalId."""
        }
    )
    uniform token aeco:phase = "proposed" (
        allowedTokens = ["proposed", "existing", "demolished", "temporary"]
        customData = {
            string userDocBrief = """Lifecycle phase of this spatial container with respect
        to this project — the asset clock (E12) on the spatial
        structure itself, so a renovation models existing fabric and
        the proposed remodel in ONE structure: an existing wing, a
        proposed space, a temporary works enclosure."""
        }
    )
    rel proxyPrim (
        customData = {
            string userDocBrief = '''The proxyPrim relationship allows us to link a
        prim whose purpose is "render" to its (single target)
        purpose="proxy" prim.'''
        }
    )
    uniform token purpose = "default" (
        allowedTokens = ["default", "render", "proxy", "guide"]
        customData = {
            string userDocBrief = """Purpose is a classification of geometry into categories that 
        can each be independently included or excluded from traversals of prims 
        on a stage, such as rendering or bounding-box computation traversals."""
        }
    )
    token visibility = "inherited" (
        allowedTokens = ["inherited", "invisible"]
        customData = {
            string userDocBrief = '''Visibility is meant to be the simplest form of "pruning" 
        visibility that is supported by most DCC apps.'''
        }
    )
    uniform token[] xformOpOrder (
        customData = {
            string userDocBrief = """Encodes the sequence of transformation operations in the
        order in which they should be pushed onto a transform stack while
        visiting a UsdStage's prims in a graph traversal that will effect
        the desired positioning for this prim and its descendant prims."""
        }
    )
}

class "AecoGroupBase" (
    apiSchemas = ["CollectionAPI:members"]
    customData = {
        string userDocBrief = "Abstract root of the two group types."
    }
)
{
    string aeco:id = "" (
        customData = {
            string userDocBrief = """Stable unique identity (lowercase hyphenated UUID
        recommended) — groups are addressable artifacts (systems in O&M
        registries, compartments in fire strategies)."""
        }
    )
    uniform token collection:members:expansionRule = "explicitOnly" (
        customData = {
            string userDocBrief = """Schema-level fallback: membership is exactly the
        targeted prims."""
        }
    )
}

class AecoSystem "AecoSystem" (
    apiSchemas = ["CollectionAPI:members"]
    customData = {
        string userDocBrief = """A functional service network — a piping run, an air loop,
    a circuit, a structural frame, a containment route: the LOGICAL
    view of a service, distinct from its spatially-placed members."""
    }
)
{
    string aeco:id = "" (
        customData = {
            string userDocBrief = """Stable unique identity (lowercase hyphenated UUID
        recommended) — groups are addressable artifacts (systems in O&M
        registries, compartments in fire strategies)."""
        }
    )
    rel aeco:serves (
        customData = {
            string userDocBrief = """The spatial prims and/or zones this system functionally
        serves — coarser than member connectivity and valid without any
        modeled connectivity (a cold water system serves a building
        before its pipes are drawn)."""
        }
    )
    uniform token collection:members:expansionRule = "explicitOnly" (
        customData = {
            string userDocBrief = """Schema-level fallback: membership is exactly the
        targeted prims."""
        }
    )
}

class AecoZone "AecoZone" (
    apiSchemas = ["CollectionAPI:members"]
    customData = {
        string userDocBrief = """A purpose-driven grouping of spaces/regions and/or
    elements — a fire compartment, an HVAC zone, a security zone, a
    department."""
    }
)
{
    string aeco:id = "" (
        customData = {
            string userDocBrief = """Stable unique identity (lowercase hyphenated UUID
        recommended) — groups are addressable artifacts (systems in O&M
        registries, compartments in fire strategies)."""
        }
    )
    uniform token collection:members:expansionRule = "explicitOnly" (
        customData = {
            string userDocBrief = """Schema-level fallback: membership is exactly the
        targeted prims."""
        }
    )
}

class AecoPort "AecoPort" (
    customData = {
        string userDocBrief = """A typed connection point on an element, authored as a child
    prim so it has a transform (the physical connection location) and
    can be targeted by relationships."""
    }
)
{
    rel aeco:connectedPorts (
        customData = {
            string userDocBrief = """The AecoPort prim(s) this port is physically connected
        to."""
        }
    )
    uniform token aeco:flowDirection = "undefined" (
        allowedTokens = ["source", "sink", "bidirectional", "undefined"]
        customData = {
            string userDocBrief = """Direction of flow at this port relative to its owning
        element: 'source' emits, 'sink' receives, 'bidirectional' does
        both (IFC SOURCEANDSINK)."""
        }
    )
    string aeco:id = "" (
        customData = {
            string userDocBrief = """Stable unique identity (ports are addressable for
        commissioning and fabrication records)."""
        }
    )
    uniform token aeco:medium = "undefined" (
        allowedTokens = ["pipe", "duct", "cable", "cableCarrier", "wireless", "other", "undefined"]
        customData = {
            string userDocBrief = """What passes or connects at this port (vocabulary
        aligned with IfcDistributionPortTypeEnum)."""
        }
    )
    rel proxyPrim (
        customData = {
            string userDocBrief = '''The proxyPrim relationship allows us to link a
        prim whose purpose is "render" to its (single target)
        purpose="proxy" prim.'''
        }
    )
    uniform token purpose = "guide" (
        allowedTokens = ["default", "render", "proxy", "guide"]
        customData = {
            string userDocBrief = """Ports render as guide geometry by default (fallback
        override of UsdGeomImageable purpose): connection points are
        authoring/coordination aids, not model geometry, so vanilla
        viewers hide them unless guides are enabled."""
        }
    )
    token visibility = "inherited" (
        allowedTokens = ["inherited", "invisible"]
        customData = {
            string userDocBrief = '''Visibility is meant to be the simplest form of "pruning" 
        visibility that is supported by most DCC apps.'''
        }
    )
    uniform token[] xformOpOrder (
        customData = {
            string userDocBrief = """Encodes the sequence of transformation operations in the
        order in which they should be pushed onto a transform stack while
        visiting a UsdStage's prims in a graph traversal that will effect
        the desired positioning for this prim and its descendant prims."""
        }
    )
}

class "AecoProjectAPI" (
    customData = {
        string userDocBrief = """Applied to the root model prim of an AECO stage: marks it
    as an AECO project and carries project identity."""
    }
)
{
    string aeco:project:id = "" (
        customData = {
            string userDocBrief = """Stable project identifier (UUID recommended) for
        cross-tool and cross-repository correlation; the counterpart
        of IfcProject.GlobalId."""
        }
    )
    string aeco:project:name = "" (
        customData = {
            string userDocBrief = "Human-readable project name."
        }
    )
}

class "AecoElementAPI" (
    customData = {
        string userDocBrief = """The universal marker for 'this prim is a built element' —
    a trait applied to any Imageable (Mesh, assembly, reference, future
    BREP), never a parallel typed hierarchy."""
    }
)
{
    string aeco:id = "" (
        customData = {
            string userDocBrief = """Stable, globally unique element identity (lowercase
        hyphenated UUID recommended) — the interchange join key, the
        counterpart of IfcRoot.GlobalId in canonical form."""
        }
    )
    uniform token aeco:phase = "proposed" (
        allowedTokens = ["proposed", "existing", "demolished", "temporary"]
        customData = {
            string userDocBrief = """Lifecycle phase of the element with respect to this
        project (the asset clock, E12)."""
        }
    )
    rel aeco:referencedContainers (
        customData = {
            string userDocBrief = """Secondary spatial anchors: containers of ANY spatial
        type, in ANY facility, that this element serves or spans beyond
        its one primary container — a riser referencing upper levels, a
        skybridge referencing the other tower's part, a culvert serving
        the adjacent road section (the bijective counterpart of
        IfcRelReferencedInSpatialStructure)."""
        }
    )
}

class "AecoClassificationAPI" (
    customData = {
        string userDocBrief = '''Multiple-apply classification hook — THE kind mechanism of
    the core: each instance binds the prim to one entry of one external
    classification system — apiSchemas = ["AecoClassificationAPI:ifc",
    "AecoClassificationAPI:uniclass"] yields aeco:class:ifc:code etc.'''
    }
)
{
    string aeco:class:__INSTANCE_NAME__:code = "" (
        customData = {
            string userDocBrief = "The code within this instance's system, e.g."
        }
    )
    string aeco:class:__INSTANCE_NAME__:name = "" (
        customData = {
            string userDocBrief = "Human-readable name of the classification entry."
        }
    )
    string aeco:class:__INSTANCE_NAME__:uri = "" (
        customData = {
            string userDocBrief = """Resolvable dictionary URI (bSDD recommended), pinning
        the exact system version."""
        }
    )
}

class "AecoTypeAPI" (
    customData = {
        string userDocBrief = """Marks a class prim as a catalog type (product/family
    definition) and carries product identity."""
    }
)
{
    string aeco:type:catalogUri = "" (
        customData = {
            string userDocBrief = "Resolvable product/catalog entry URI."
        }
    )
    string aeco:type:manufacturer = "" (
        customData = {
            string userDocBrief = """Manufacturer or system owner of the cataloged
        product/build-up."""
        }
    )
    string aeco:type:model = "" (
        customData = {
            string userDocBrief = "Model/product designation."
        }
    )
}

class "AecoAxisAPI" (
    customData = {
        string userDocBrief = """The driving axis of a path-based element (wall, pipe, duct,
    cable carrier, beam, column): the one representation every host
    treats as THE parameter — IFC's 'Axis' representation, an authoring
    tool's location curve."""
    }
)
{
    double3 aeco:axis:arcPoint = (0, 0, 0) (
        customData = {
            string userDocBrief = """DRIVER, arc only: a point on the arc between start and
        end."""
        }
    )
    uniform token aeco:axis:curve = "line" (
        allowedTokens = ["line", "arc"]
        customData = {
            string userDocBrief = "DRIVER."
        }
    )
    double3 aeco:axis:end = (1, 0, 0) (
        customData = {
            string userDocBrief = "DRIVER (m)."
        }
    )
    double aeco:axis:length = 0 (
        aecoDerived = true
        customData = {
            string userDocBrief = "DERIVED (m)."
        }
    )
    double3 aeco:axis:start = (0, 0, 0) (
        customData = {
            string userDocBrief = "DRIVER (m)."
        }
    )
}

class "AecoDerivedGeometryAPI" (
    customData = {
        string userDocBrief = """Marks a gprim as DERIVED from a referent by a tool: the
    host's tessellated body, a stage-side proxy, the axis curve, a
    footprint, a symbol, a spatial extent or an analytic guide."""
    }
)
{
    uniform token aeco:derived:approx = "exact" (
        allowedTokens = ["exact", "tessellated", "arcSegmented", "defaultDims", "bbox"]
        customData = {
            string userDocBrief = "How far the picture is from the truth it derives from."
        }
    )
    uniform token aeco:derived:role = "body" (
        allowedTokens = ["body", "proxy", "axis", "footprint", "symbol", "extent", "sector", "coverage"]
        customData = {
            string userDocBrief = """What the gprim is a picture of: the element's body
        (purpose render), a stand-in (purpose proxy), the axis (purpose
        guide), a plan footprint, a symbol, a spatial extent (never an
        obstacle), an analytic view sector or analytic clipped coverage."""
        }
    )
    string aeco:derived:source = "" (
        customData = {
            string userDocBrief = "aeco:id of the element or spatial referent this gprim stands for."
        }
    )
    string aeco:derived:stamp = "" (
        customData = {
            string userDocBrief = "The deriving host or tool and its version."
        }
    )
}

